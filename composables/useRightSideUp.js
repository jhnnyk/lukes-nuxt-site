// code for Upside down button that you click when right side up.

// This doesn't work when used in this separate .js file, the code only works in-line, I think because otherwise my website doesn't know where to find the document due to all my weird Nuxt stuff.

export function useRightSideUp() {
    // document.getElementById('whole').classList.add('upsidedown'); 
    // document.getElementById('rightsideup').style.display='none'; 
    // document.getElementById('upsidedown').style.display='inline'; 
    // document.getElementById('upmessage').style.display='inline';
}