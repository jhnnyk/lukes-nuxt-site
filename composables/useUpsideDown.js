// Code for upside down button that you click when already upside down.

// This doesn't work when used in this separate .js file, the code only works in-line, I think because otherwise my website doesn't know where to find the document due to all my weird Nuxt stuff.

export function useUpsideDown() {
    // document.getElementById('whole').classList.remove('upsidedown'); 
    // document.getElementById('upsidedown').style.display='none'; 
    // document.getElementById('rightsideup').style.display='inline'; 
    // document.getElementById('upmessage').style.display='none'
}
